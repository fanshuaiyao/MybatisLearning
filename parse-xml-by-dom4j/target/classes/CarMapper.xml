<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.mybatis.example.CarMapper">

    <insert id="insertCar">
        INSERT INTO t_car (id,car_num, brand, guide_price, produce_time, car_type)
        VALUES (null, #{carNum},#{brand}, #{guidePrice}, #{produceTime}, #{carType});
    </insert>

    <!-- 更新汽车价格 -->
    <update id="updateCarPrice" parameterType="map">
        UPDATE t_car
        SET guide_price = #{guidePrice}
        WHERE id = #{id};
    </update>

    <!-- 删除指定ID的汽车 -->
    <delete id="deleteCarById" parameterType="int">
        DELETE FROM t_car WHERE id = #{id};
    </delete>

    <select id="selectAll" resultType="com.fan.mybatis.pojo.Car">
        select
            id,
            car_num as carNum,
            brand,
            guide_price as guidePrice,
            produce_time as produceTime,
            car_type as carType
        from t_car
    </select>
</mapper>
